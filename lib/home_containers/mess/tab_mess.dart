import 'package:flutter/material.dart';

class tabMess extends StatelessWidget {
  const tabMess({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('tab message'),
      ),
    );
  }
}
